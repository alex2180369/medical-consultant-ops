services:
  dozzle:
    image: amir20/dozzle:latest
    container_name: medical_consultant_dozzle
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    ports:
      - "127.0.0.1:9999:8080"
    environment:
      DOZZLE_NO_ANALYTICS: "true"
